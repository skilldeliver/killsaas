// Legacy file that re-exports everything from the new API structure
// This maintains backward compatibility while we transition to a better organized code structure

// Re-export everything from our new API modules
export * from './api';
